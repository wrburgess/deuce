#!/usr/bin/env bash
# bin/factory-install — arm the trigger; --remove disarms it.
# The only sanctioned writer of the installed launchd agent (config/factory.md).
set -euo pipefail
cd "$(dirname "$0")/.."
exec node tools/factory/install.ts "$@"
