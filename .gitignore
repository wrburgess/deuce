# === macOS ===
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# === Windows ===
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.stackdump
*.lnk

# === Linux ===
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# === Editors / IDEs ===
.vscode/
.idea/
*.swp
*.swo

# === Node ===
node_modules/

# === deuce ===
# An un-posted review record, saved by tools/review/summon.ts when a post fails.
# It exists for the HC to place by hand; it is never committed.
deuce-unposted-*

# Session worktrees live under .claude/, which now also holds tracked content
# (.claude/skills/, ADR 0027) — this line keeps them out of any commit.
.claude/worktrees/
